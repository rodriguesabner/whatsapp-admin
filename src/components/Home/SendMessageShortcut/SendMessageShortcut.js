import Carousel from 'vue-owl-carousel';
import {
  Layout, PlusButton, ScheduledList, UsersList, UserCard, TitleComponent, TopContent, TopSeparator,
} from './styles';

export default {
  name: 'Stats',
  components: {
    Layout,
    UsersList,
    ScheduledList,
    PlusButton,
    Carousel,
    UserCard,
    TitleComponent,
    TopContent,
    TopSeparator,
  },
};
